import 'package:flutter/material.dart';
class UserReals extends StatelessWidget
{
  const UserReals({Key? key}):super(key: key);
  @override
  Widget build(BuildContext context)
  {
    return Scaffold(
      body: Center(child:Text('Reals'),),

    );
  }
}